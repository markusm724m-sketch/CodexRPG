[project]
name = "codexrpg"
version = "0.1.0"
description = "Fantasy isekai RPG sandbox"
readme = "README.md"
authors = ["Unknown"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
